Using (13) and (18) we see that
lim
p→∞
log(ℓp)
log(p) = lim
p→∞
log(ℓp)
log L(ℓp)
log L(ℓp)
log(p)
=
1
ρ[L] = ω(L) = ω(M).
As M and L are strongly regular, we have m ≃l, i.e.
there exists a > 1 such that
a−1mp ≤lp ≤amp for every p ∈N0. It is clear then that
lim
p→∞log(mp)/ log(p) = lim
p→∞log(ℓp)/ log(p) = ω(M).
Conversely, let us assume now that limp→∞log(mp)/ log(p) = ω(M). As before, this
amounts to
lim
p→∞
log(ℓp)
log(p) = ω(M).
Using (14) and (15), for every t ∈(ℓp−1, ℓp) and p large enough, we see that
log(p −1) + log(ε)
log(ℓp)
≤log(L(ℓp−1))
log(ℓp)
≤log(L(t))
log(t)
≤log(L(ℓp))
log(ℓp−1) ≤log(p) + log(log(a))
log(ℓp−1)
.
Taking limits we deduce that limt→∞dL(t) = 1/ω(M), and thanks to (17) we conclude that
limt→∞dM(t) = 1/ω(M), as desired.
In the next result we assume that condition (C) for dM holds, and obtain a characteri-
zation of condition (D). This result follows the ideas in the proof of Proposition 4.9 in [50],
but takes into account that dM need not to be monotone, as it was thought to be true at
that moment.
Proposition 3.5. Let M be a strongly regular sequence. If we assume that limt→∞dM(t) =
1/ω(M), the following are equivalent:
(i) dM(t) is a proximate order, i.e., it veriﬁes (D),
(ii)
lim
p→∞
p
M(mp) =
1
ω(M) = ρ[M].
Proof. We have that
d′
M(t) =
M ′(t)
log(t)M(t) −
dM(t)
t(log(t))
whenever it exists. If t ∈(mp−1, mp),
d′
M(t) =
p
t log(t)M(t) −dM(t)
t log(t) =
1
t log(t)
 ν(t)
M(t) −dM(t)

,
where ν(t) is the function deﬁned in (9). For convenience, write b(t) = td′
M(t) log(t). Then
limt→∞b(t) = 0 (i.e., (D) is satisﬁed) if, and only if,
lim
t→∞
 ν(t)
M(t) −dM(t)

= 0.
Since we are assuming that limt→∞dM(t) = 1/ω(M), the previous equality holds if, and
only if,
lim
t→∞
ν(t)
M(t) =
1
ω(M).
(19)
Observe that M(t) is nondecreasing, and ν(t) is constant in (mp−1, mp), so ν(t)/M(t) is
nonincreasing in (mp−1, mp) and we deduce that
p
M(mp) ≤ν(t)
M(t) ≤
p
M(mp−1) =
p −1
M(mp−1) +
1
M(mp−1),
t ∈(mp−1, mp).
12
